import { nodeResolve } from "@rollup/plugin-node-resolve";
<%- transformCommonJS ? 'import commonjs from "@rollup/plugin-commonjs";' : ""%>

export default {
  input: "src/index.js",
  output: {
    dir: "dist",
    format: "esm",
  },
  plugins: [
    <%= transformCommonJS ? "commonjs()," : ""%>
    nodeResolve(),
  ],
};
