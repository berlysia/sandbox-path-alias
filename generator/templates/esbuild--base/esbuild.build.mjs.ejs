import esbuild from "esbuild";

esbuild.build({
  entryPoints: ["src/index.<%= sourceExt %>"],
  outdir: "dist",
  bundle: true,
  platform: "node",
  format: "<%= outputFormat %>",
<% if (useAlias) { %>
  alias: {
    "##": "./src",
    "#deep": "./src/deep",
  },
<% } %>
});
